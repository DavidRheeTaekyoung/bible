# 이어받기

**마지막 세션**: 2026-09-20 · 클라우드 세션(휴대폰). 다음부터는 PC `C:\100\bible` 에서 이어간다.

## 된 것

- 레포 `DavidRheeTaekyoung/bible` (Public) · MkDocs Material · `main` 에 밀면 Actions 가 `gh-pages` 에 올린다. 첫 배포 성공, `gh-pages` 에 `CNAME` 있음.
- 규율: `CLAUDE.md` · `docs/연구규율.md` · `docs/템플릿.md` (주장 → 근거의 무게 → 검토 → 판정).
- 주제 계획 셋: `주제/메시아예언/` · `주제/신약기록순서.md` · `주제/창조와현대과학.md` — 각각 「할 일」 체크리스트 있음.

## 안 된 것 — 사이트가 아직 안 뜬다

1. GitHub Settings → Pages: Source = branch `gh-pages`, `/ (root)`. Custom domain `bible.busangames.com`.
2. 클라우드플레어 `busangames.com` DNS: CNAME `bible` → `davidrheetaekyoung.github.io`, **DNS only(회색 구름)**.
3. 인증서 잡히면 Enforce HTTPS. `https://bible.busangames.com` 200 확인.

집 PC 「디시전메이커」 세션에 12:12 에 이 셋을 시켰는데 결과 미확인. **그 세션 화면의 「bible 설정 결과:」 줄을 먼저 보고**, 없으면 위 셋을 직접 한다. 확인 뒤 이 절을 지운다.

## 확인 전 표시

주제 페이지의 참고 문헌 연도·서지, 어휘 목록의 절 위치는 기억으로 적은 것이다. 「(확인 전)」 을 떼기 전에 하나씩 확인한다.
